{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ejecutar programa",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/programa.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Habilitar impresión de formato bonito",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Compilar Todo"
        }
    ]
}
